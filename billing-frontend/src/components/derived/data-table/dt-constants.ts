/** `app_entity_screen_column.column_component` for the sticky row actions column. */
export const DT_COLUMN_COMPONENT_ACTIONS = 'actions';

/** Entity field names for row state (not grid columns); same on all master entities. */
export const DT_FIELD_IS_ENABLED = 'is_enabled';
export const DT_FIELD_IS_ACTIVE = 'is_active';
